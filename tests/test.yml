---
- hosts: localhost
  remote_user: root
  vars:
    - users:
      - name: "sftptestuser1"
        password: "{{ "THISROCKS" | password_hash('sha512') }}"
  roles:
    - role: sftp
      sftp_users: "{{ users }}"
      sftp_additional_directories:
        - test1
        - test2
        - test3